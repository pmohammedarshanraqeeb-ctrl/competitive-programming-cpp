#include<bits/stdc++.h>
using namespace std;
int main(){
    int a = 1 ;
    int b =2;
    int c =3;
    // cout<<((a<=b)||(b<=a))<<endl;
    cout<<((c>b)&&(b>c))<<endl;
    /*
    this logical operator is based on logic gates such as AND and OR gates when its AND its multiplication between 1 and 0 and when its OR its addition between 1and o 
    if you want to know more about this search for logic gates
    thankyou 
    */
}